<!-- Add you custom twig html here -->
<div {{ attributes }}>
  {% for key,items in columns %}
    <div class="col col-{{ key }}">
      {% for item in items %}
        <div class="search-block">
          <h2>{{ item.title }}</h2>
          {{ item.content }}
          {{ item.link }}
        </div>
      {% endfor %}
    </div>
  {% endfor %}
</div>

