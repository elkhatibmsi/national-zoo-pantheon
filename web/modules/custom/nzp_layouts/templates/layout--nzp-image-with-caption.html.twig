{#
this figure component reads the current page's path and then sets the 
border color based on that path. E.g. if the page is in the visit section then
the figcaption gets an orange border.
#}

<div {{attributes}}>
<figure class="bg-oxford-blue w-full image-caption-title-container m-0">
	<div  {{ region_attributes.image }} class="image-caption-image w-full">{{ content.image }}</div>
	<figcaption class="text-white ml-2 pb-2">
		<div  {{ region_attributes.caption }} class="image-caption-title ml-2">
			{{ content.caption }}</div>
	</figcaption>
</figure>
</div>
